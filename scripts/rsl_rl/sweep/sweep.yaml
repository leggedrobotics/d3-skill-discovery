# https://docs.wandb.ai/guides/sweeps/sweep-config-keys/#distribution-options-for-random-and-bayesian-search
program: unsupervised_RL/scripts/rsl_rl/train.py
# program: scripts/rsl_rl/train.py
description: "Sweep over hyperparameters for factorized usd (metra, diayn) ppo anymal"
method: bayes  # or 'grid', 'random'
metric:
  name: Factor_Metrics/position  # wandb metric to optimize
  goal: maximize  # 'minimize' or 'maximize' if optimizing a loss
parameters:
# # agent
  agent.seed:
    min: 0
    max: 1000
# # usd
  agent.algorithm.symmetry_augmentation:
    values: [False]
  # agent.usd.disable_style_reward:
  #   values: [True, False]
  # agent.usd.disable_regularization:
  #   values: [True, False]
  agent.usd.disable_factor_weighting:
    values: [True, False]

  # env.episode_length_s:
  #   values: ["15.0", "30.0"]


#   agent.policy.actor_layers:
#     values: [[256, 128, 128], [512, 256, 256], [512, 512, 512]]
#   # agent.policy.architecture:
#   #   values: ["MLP", "MLP", "SimBa"]
  # agent.usd.factor_weights_skew:
  #   values: ["1.0", "1.5"]
#   agent.num_steps_per_env:
#     values: [25]




#   - --task=Isaac-USD-Anymal-D-v0 
#   - --task=Isaac-USD-AnymalBox-D-v0
#   - --task=Isaac-USD-AnymalBoxes-D-v0
#   - --task=Isaac-USD-AnymalRough-D-v0
#   - --task=Isaac-USD-HL-Anymal-D-v0
#   - --task=Isaac-USD-HL-Box-Anymal-D-v0


command:
  - /isaac-sim/python.sh
  - ${program}
  - --task=Isaac-USD-AnymalRough-D-v0
  - --num_envs=2048 #16384 8192 4096 2048 
  - --logger=wandb
  - --headless
  # - --enable_cameras
  # - --resume=RESUME
  # - --load_run=usd_mddd_blind


  - ${args_no_hyphens}

  # - --video
  # - --video_length=400
  # - --video_interval=150000
